// make hamburger menu, niceniceniceniceniceniiiiice!!!

function makeHamburgerMenu() {
  // wire up the hamburger menu with click events
}

function openCloseHamburger(isOpen) {
  // close hamburger if open
  // open hamburger if closed
}

function addRemoveToppings(isOpen) {
  // identify which topping was tapped
  // close topping if open
  // open topping if closed
}
